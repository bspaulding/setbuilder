version: '3'
services:
  web:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - .:/usr/src/app
    links:
      - redis
    environment:
      REDIS_URL: 'redis://redis:6379'
  redis:
    image: redis:alpine
